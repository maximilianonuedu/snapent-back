{
    "version":2,
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
    "builds":[
        {
            "src": "./expressServer.js",
            "use":"@vercel"
        }
    ],
    "routes":[
        {
            "src": "/(.*",
            "dest": "/"
        }
    ]
}